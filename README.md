# onemind.md
onemind.md turns any git repo into a persistent "mind" using zero files on disk. Thoughts live as git objects on a hidden ref (refs/mind/main) — written via plumbing, invisible to git status. You get decisions with reasons, full history/diff/blame on your thinking, and it all clones with the repo. No database, no service, no API key — just git.
